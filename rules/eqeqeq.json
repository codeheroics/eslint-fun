{
  "name": "eqeqeq",
  "description": "require the use of `===` and `!==`",
  "isRecommended": false,
  "isFixable": false,
  "category": "Best Practices",
  "schema": [
    {
      "enum": [
        "smart",
        "allow-null"
      ]
    }
  ]
}